{
  "name": "potyk-io",
  "version": "1.0.0",
  "description": "Бложек имени @potykion",
  "scripts": {
    "tailwind-watch": "npx tailwindcss -i docs/a/tailwind-layers.css -o ./docs/a/tailwind-output.css --watch",
    "tailwind": "npx tailwindcss -i docs/a/tailwind-layers.css -o ./docs/a/tailwind-output.css",
    "tinacms": "npx tinacms dev -c \"mkdocs serve\"\n"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
